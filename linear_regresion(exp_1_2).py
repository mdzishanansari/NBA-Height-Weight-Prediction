# -*- coding: utf-8 -*-
"""Linear Regresion(Exp-1.2).ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1P3Lor2ucFU_gwyJFLw_r9vSGtLfQGYAV
"""

# Commented out IPython magic to ensure Python compatibility.
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
# %matplotlib inline
from sklearn import linear_model

df = pd.read_csv('weight-height.csv')

df.shape

df

df.plot(kind='scatter', x='Height', y='Weight')

data = df.iloc[:500, :]

data

data.plot(kind='scatter', x='Height', y='Weight')

data.shape

data.plot(kind='box')

data.corr()

height = pd.DataFrame(data['Height'])
weight = pd.DataFrame(data['Weight'])

height

weight

LM = linear_model.LinearRegression()
model = LM.fit(height, weight)

model.coef_

model.intercept_

model.score(height, weight)

height_new = [[65.483290]]
weight_predict = model.predict(height_new)
weight_predict

x = ([64.382, 50.1245, 68.88888])
x = pd.DataFrame(x)
y = model.predict(x)
y = pd.DataFrame(y)
result = pd.concat([x, y], axis = 1, keys = ['Height_new', 'Weight_new'])

result

data.plot(kind='scatter', x='Height', y='Weight')
plt.plot(height, model.predict(height), color = 'red', linewidth=2)

result.plot(kind='scatter', x='Height_new', y='Weight_new')
plt.plot(height, model.predict(height), color = 'red', linewidth=2)



